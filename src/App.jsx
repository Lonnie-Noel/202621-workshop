export default function App() {
  return (
    <main aria-labelledby="app-title">
      <h1 id="app-title">Orchestration Dashboard Bootstrap</h1>
      <p>Tooling shell is ready for the dashboard implementation.</p>
    </main>
  );
}