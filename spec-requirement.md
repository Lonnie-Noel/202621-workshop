# 멀티 에이전트 오케스트레이션 관리 대시보드 요구사항

## 1. 목적

React와 Node.js를 사용해 여러 에이전트의 상태, 담당 작업, 실행 로그, 모델 사용 판단 기준을 한 화면에서 확인하는 관리 대시보드를 만든다.

이 문서는 구현자가 바로 작업을 쪼갤 수 있도록 핵심 요구사항, MVP 범위, 제외 범위, 추가하면 좋은 기능, 작은 작업 단위 전략을 정의한다.

## 2. 대상 사용자

- 여러 에이전트에게 작업을 나누고 진행 상황을 확인하는 오케스트레이션 담당자
- 에이전트별 역할, 상태, 산출물을 빠르게 점검해야 하는 프로젝트 운영자
- 작은 모델 또는 주니어 에이전트에게 명확한 단위 작업을 위임하려는 작업 관리자

## 3. 핵심 사용자 시나리오

1. 사용자는 대시보드에 들어와 현재 등록된 에이전트 목록과 상태를 확인한다.
2. 사용자는 진행 중인 작업 목록에서 작업 요약, 상태, 담당 에이전트를 확인한다.
3. 사용자는 특정 작업이나 에이전트의 실행 로그와 메모를 확인한다.
4. 사용자는 모델 사용 전략 체크리스트를 보며 어떤 작업을 작은 모델, 큰 모델, 위원회식 판단 흐름 중 어디에 배정할지 결정한다.
5. 사용자는 다음 작업자가 바로 이어받을 수 있을 만큼 현재 상태와 차단 이슈를 파악한다.

## 4. 핵심 기능 요구사항

### 4.1 에이전트 카드 영역

- 각 에이전트를 카드 형태로 보여준다.
- 카드에는 에이전트 이름, 역할, 현재 상태, 담당 작업 수, 최근 활동 시간이 포함되어야 한다.
- 상태는 최소한 `대기`, `진행 중`, `검토 중`, `차단`, `완료`를 구분한다.
- 상태별 시각 구분이 가능해야 한다.
- MVP에서는 실제 실시간 연동이 없어도 정적 또는 목 데이터로 화면을 구성할 수 있다.

### 4.2 작업 상태 목록 영역

- 작업 목록을 표 또는 리스트 형태로 보여준다.
- 각 작업에는 작업 제목, 짧은 요약, 상태, 담당 에이전트, 우선순위가 포함되어야 한다.
- 상태는 최소한 `대기`, `진행 중`, `검토 필요`, `차단`, `완료`를 구분한다.
- 사용자는 어떤 작업이 누구에게 배정되었고 다음에 무엇을 봐야 하는지 알 수 있어야 한다.
- MVP에서는 정렬, 필터, 검색은 필수가 아니다.

### 4.3 로그 및 메모 패널

- 에이전트 실행 로그 또는 운영 메모를 시간순으로 보여준다.
- 각 항목에는 시간, 관련 에이전트, 관련 작업, 메시지, 중요도 또는 유형이 포함되어야 한다.
- 유형은 최소한 `정보`, `경고`, `오류`, `메모`를 구분한다.
- MVP에서는 로그 추가 입력 기능 없이 샘플 로그 표시만으로 시작할 수 있다.

### 4.4 모델 사용 전략 체크리스트

- 작업을 어떤 모델 또는 의사결정 방식에 맡길지 판단하는 체크리스트를 제공한다.
- 체크리스트에는 다음 기준이 포함되어야 한다.
- 작업이 10-20분 안에 끝나는가?
- 입력과 출력이 명확한가?
- 파일 수정 범위가 제한적인가?
- 실패해도 되돌리기 쉬운가?
- 복잡한 설계 판단이나 보안 판단이 필요한가?
- 여러 의견을 비교해야 하는가?
- 결과를 리뷰할 사람이 있는가?

### 4.5 위원회식 결정 흐름

- 모델 사용 전략 영역에는 단순 체크리스트뿐 아니라 위원회식 판단 흐름을 포함한다.
- 기본 흐름은 `작은 모델 초안 작성` -> `전문 에이전트 검토` -> `오케스트레이션 리드 결정` -> `최종 실행 또는 보류`로 정의한다.
- 큰 모델 또는 복수 에이전트 검토가 필요한 조건을 명시한다.
- 예시는 다음과 같다.
- 보안, 권한, 데이터 손실 가능성이 있으면 큰 모델 또는 보안 에이전트 검토가 필요하다.
- 사용자 경험이나 정보 구조 판단이 크면 디자이너 또는 제품 분석 에이전트 검토가 필요하다.
- 단순 UI 카드 추가, 정적 데이터 표시, 문구 정리는 작은 모델에 위임할 수 있다.

## 5. MVP 권장 범위

### 5.1 MVP에 포함할 것

- React 기반 단일 대시보드 화면
- Node.js 기반 간단한 API 또는 목 데이터 제공 구조
- 에이전트 카드 4-8개 샘플 표시
- 작업 상태 목록 5-10개 샘플 표시
- 로그 또는 메모 패널 샘플 표시
- 모델 사용 전략 체크리스트 표시
- 위원회식 의사결정 흐름 표시
- 로딩, 빈 상태, 오류 상태의 최소 표현 기준 문서화

### 5.2 MVP에서 단순화할 것

- 데이터는 처음에는 정적 JSON 또는 서버 메모리 데이터로 시작한다.
- 인증과 권한은 실제 구현 대신 향후 요구사항으로 남긴다.
- 실시간 업데이트는 자동 갱신이나 WebSocket 없이 수동 새로고침 또는 정적 표시로 시작한다.
- 로그 입력, 작업 생성, 에이전트 등록은 읽기 전용 화면 이후 단계로 미룬다.

## 6. 비목표

- 실제 에이전트 실행 엔진 구현
- 에이전트 작업 자동 배정 알고리즘 구현
- 복잡한 인증, 조직 권한, 결제, 계정 관리
- 장기 로그 저장소와 검색 엔진 구축
- 실시간 WebSocket 기반 모니터링 완성
- 모델 API 직접 호출 또는 비용 정산 기능
- 모든 에이전트 역할의 상세 업무 자동화
- 운영 환경 배포 자동화 완성

## 7. 유용한 추가 기능 제안

### 7.1 우선순위 높은 추가 기능

- 상태 필터: `차단`, `검토 필요`, `진행 중`만 빠르게 보기
- 작업 상세 패널: 선택한 작업의 설명, 담당자, 로그, 다음 행동 표시
- 차단 이슈 표시: 사람이 승인하거나 결정해야 하는 항목 강조
- QA 체크리스트: 핵심 흐름이 검증되었는지 표시

### 7.2 나중에 추가할 기능

- 간단한 작업 생성 및 상태 변경
- 에이전트별 성과 지표: 완료 작업 수, 평균 처리 시간, 차단 횟수
- 로그 검색 및 유형 필터
- 실시간 업데이트
- 권한별 화면 제한
- 감사 로그와 데이터 보존 정책
- 실패 작업 재시도 또는 후속 작업 생성 흐름

## 8. 작은 작업 단위 분해 전략

작은 모델 또는 주니어 에이전트에게 줄 작업은 10-20분 안에 끝나야 한다. 각 작업은 입력, 출력, 완료 기준이 명확해야 하며 한 번에 한 파일 또는 한 컴포넌트 중심으로 제한하는 것을 권장한다.

### 8.1 작업 분해 원칙

- 한 작업은 하나의 화면 영역 또는 하나의 데이터 구조만 다룬다.
- 작업 설명에는 수정 대상, 필요한 데이터 필드, 완료 기준을 적는다.
- 디자인 판단이 필요한 작업과 데이터 구조 작업을 섞지 않는다.
- API 작업과 UI 작업을 분리한다.
- 테스트 또는 검증 작업은 별도 작업으로 둔다.
- 애매한 요구는 구현자에게 넘기지 말고 확인 필요 항목으로 남긴다.

### 8.2 10-20분 작업 예시

1. 에이전트 샘플 데이터 구조 정의
2. 작업 샘플 데이터 구조 정의
3. 로그 샘플 데이터 구조 정의
4. 에이전트 카드 컴포넌트 만들기
5. 에이전트 카드 목록 렌더링하기
6. 작업 상태 리스트 컴포넌트 만들기
7. 작업 상태 뱃지 스타일 정의하기
8. 로그 패널 컴포넌트 만들기
9. 모델 사용 전략 체크리스트 컴포넌트 만들기
10. 위원회식 결정 흐름 컴포넌트 만들기
11. 대시보드 레이아웃에 각 영역 배치하기
12. 빈 상태 메시지 추가하기
13. 오류 상태 메시지 추가하기
14. 간단한 API 응답 형태 정의하기
15. 프론트엔드가 API 또는 목 데이터를 읽도록 연결하기
16. 핵심 렌더링 테스트 작성하기
17. 수동 QA 체크리스트 작성하기

### 8.3 작은 모델에게 적합한 작업

- 정적 데이터 표시
- 단순 카드, 리스트, 배지 컴포넌트 작성
- 상태별 문구 정리
- 목 데이터 추가
- 렌더링 테스트 작성
- 문서의 체크리스트 정리

### 8.4 큰 모델 또는 위원회 검토가 필요한 작업

- 전체 정보 구조 결정
- 데이터 모델과 API 경계 결정
- 권한, 감사 로그, 민감 정보 처리 결정
- 실시간 모니터링 구조 결정
- 여러 에이전트의 책임 충돌 조정
- 실패 복구와 운영 절차 결정

## 9. 데이터 필드 초안

### 9.1 Agent

- `id`: 에이전트 식별자
- `name`: 에이전트 이름
- `role`: 역할 이름
- `status`: 현재 상태
- `currentTaskCount`: 담당 중인 작업 수
- `lastActiveAt`: 최근 활동 시간
- `summary`: 짧은 역할 설명

### 9.2 Task

- `id`: 작업 식별자
- `title`: 작업 제목
- `summary`: 작업 요약
- `status`: 작업 상태
- `priority`: 우선순위
- `assignedAgentId`: 담당 에이전트 식별자
- `updatedAt`: 최근 변경 시간

### 9.3 LogMemo

- `id`: 로그 또는 메모 식별자
- `timestamp`: 발생 시간
- `agentId`: 관련 에이전트 식별자
- `taskId`: 관련 작업 식별자
- `type`: 정보, 경고, 오류, 메모
- `message`: 표시할 메시지

## 10. 확인 필요 항목

- 실제 로그인과 권한 관리가 MVP에 포함되는가?
- 데이터 저장소를 사용할 것인가, 아니면 초기에는 정적 JSON과 메모리 데이터로 충분한가?
- 에이전트 상태는 수동으로 바뀌는가, 실제 실행 시스템과 연동되는가?
- 로그는 사용자가 직접 입력하는 메모인가, 에이전트 실행에서 자동 수집되는가, 둘 다인가?
- 위원회식 결정 흐름은 단순 가이드 문구인가, 실제 승인 워크플로우인가?
- 우선 지원할 화면 크기는 데스크톱 중심인가, 모바일도 MVP에 포함되는가?
- 모델 사용 전략에서 실제 모델 이름과 비용 기준을 표시해야 하는가?

## 11. 완료 기준

- 대시보드의 핵심 영역인 에이전트 카드, 작업 상태 목록, 로그/메모 패널, 모델 전략 체크리스트가 요구사항에 포함된다.
- MVP 범위와 비목표가 분리되어 구현자가 과도한 기능을 만들지 않도록 한다.
- 작은 모델 또는 주니어 에이전트가 수행할 수 있는 10-20분 단위 작업 목록이 제공된다.
- 애매한 요구사항은 확인 필요 항목으로 남아 있다.
- 다음 단계에서 디자인, 아키텍처, API, 프론트엔드 구현 계획으로 이어질 수 있다.
